@model IEnumerable<OG_SLN.SET_ROLE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ROLE_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ROLE_DETAILS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IS_ACTIVE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ROLE_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ROLE_DETAILS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_ACTIVE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ROLE_NO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ROLE_NO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ROLE_NO })
            @Html.ActionLink("Permissions", "Permissions", new { id = item.ROLE_NO })
        </td>
    </tr>
}

</table>
